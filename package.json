{
  "name": "chat-app",
  "private": true,
  "workspaces": [
    "web",
    "realtime",
    "mobile",
    "shared"
  ],
  "scripts": {
    "build": "npm run -w shared build && npm run -w web build"
  },
  "devDependencies": {
    "@types/node": "^25.0.10",
    "esbuild": "^0.27.2"
  },
  "dependencies": {
    "browser-image-compression": "^2.0.2",
    "mongoose": "^9.1.6",
    "next-themes": "^0.4.6"
  }
}
